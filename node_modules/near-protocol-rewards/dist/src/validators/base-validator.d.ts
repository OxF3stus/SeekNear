import { Logger } from "../utils/logger";
import { ValidationResult, ValidationError } from "../types/validation";
import { ErrorCode } from "../types/errors";
export declare abstract class BaseValidator {
    protected readonly logger: Logger;
    constructor(logger: Logger);
    protected createValidationResult(isValid: boolean, errors: ValidationError[], warnings?: ValidationError[]): ValidationResult;
    protected createError(code: ErrorCode, message: string, context: Record<string, unknown>): ValidationError;
}
